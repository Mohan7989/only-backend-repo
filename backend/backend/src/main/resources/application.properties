# Server
server.port=8080


spring.application.name=student-resources

# MySQL database
spring.datasource.url=jdbc:mysql://localhost:3306/student_resources?useSSL=false&serverTimezone=UTC
spring.datasource.username=mohan
spring.datasource.password=password123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update   # use 'update' for dev, 'validate' for prod
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# File upload
file.upload-dir=src/main/resources/static/uploads
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

