[phases.setup]
cmds = [
    "apt-get update",
    "apt-get install -y maven"
]

[phases.build]
cmds = ["mvn clean package -DskipTests"]

[start]
cmd = "java -jar target/student-resources-0.0.1-SNAPSHOT.jar"